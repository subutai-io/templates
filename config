# Configuration - change these to match your system

# Defaults will work on a system with subutai snap installed

# A working btrfs mount point
BTRFS_ROOT=/var/snap/subutai-master/common/lxc

# A folder which is used exclusively for this - can NOT exist and WILL
# be cleaned out when done!
BTRFS_BUILD=build

# vim: ts=4 et nowrap autoindent
